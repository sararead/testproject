version: 2
jobs:
  build:
    docker:
      - image: circleci/android:api-28-ndk-r17b
    steps:
      - checkout
      - run: bash echo.sh
      - run: wait 90
      - run: echo "is this canceled?"
      
      
workflows:
  version: 2
  test-build-deploy:
    jobs:
      - build
      

